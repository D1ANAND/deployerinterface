import UploadForm from './components/UploadForm';

const Home: React.FC = () => {
    return (
        <main>
            <h1>Upload Python Script to IPFS</h1>
            <UploadForm />
        </main>
    );
};

export default Home;
